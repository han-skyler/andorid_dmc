package com.example.u2project;

public class upload {
/*
// Firebase 인스턴스 초기화
        FirebaseApp.initializeApp(this);

    FirebaseFirestore db = FirebaseFirestore.getInstance(); // Firestore 인스턴스 가져오기

    // 상위 폴더 선택
//        String Default = "";
//        Default = "OriTime";
//        Default = "OriPlace";
//        Default = "Memory";
//        Default = "Calculate";
//        Default = "Language";
//        Default = "Perception";
//        Default = "Mood";
//        Default = "Evaluation";
//        Default = "etc";
    int Start_num = 1;

    // 질문 데이터 준비
    String[] Q = {
            "50",
            "12",
            "39"
    };

    int Last_Num = Q.length;

        for (int i = Start_num ; i < Last_Num+1 ; i++){
        String Num = Integer.toString(i);
        String documentPath = Default+"/"+Num; // 문서 경로
        // 데이터 업데이트
        Map<String, Object> data = new HashMap<>();
        data.put("a", Q);
        db.document(documentPath) // 문서 경로 선택
                .update(data) // 데이터 업데이트
                .addOnSuccessListener(new OnSuccessListener<Void>() {
                    @Override
                    public void onSuccess(Void aVoid) {
                        // 업데이트 성공시 실행되는 코드
                    }
                })
                .addOnFailureListener(new OnFailureListener() {
                    @Override
                    public void onFailure(@NonNull Exception e) {
                        // 업데이트 실패시 실행되는 코드
                    }
                });
    }
*/

}
